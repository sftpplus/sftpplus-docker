The content of this directory will overwrite the SFTPPlus configuration
initialized from scratch.

Add here a custom server.ini to add your own users and your own
SSL certificates and SSH keys to the SFTPPlus container.
